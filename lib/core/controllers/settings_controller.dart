// import '../../data/models/local/settings_local_model.dart';
// import '../base/hive/base_hive.dart';
// import '../di/locator.dart';

class SettingsController {
  const SettingsController._();

  // static final _settingsHiveDataSource =
  //     locator<BaseHive<SettingsLocalModel>>();

  // static SettingsLocalModel? get settingsModel =>
  //     _settingsHiveDataSource.getData();

  // static String get language => settingsModel?.language ?? 'az';
  // static bool get onboardCompleted => settingsModel?.onboardCompleted ?? false;
  // static bool get languageSelected => settingsModel?.languageSelected ?? false;
  // static bool get isVibrationActive => settingsModel?.isVibrationActive ?? true;

  // static Future<void> saveSettingsData(SettingsLocalModel settingsData) =>
  //     _settingsHiveDataSource.saveData(settingsData);
}
