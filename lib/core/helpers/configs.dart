import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

class Configs {
  const Configs._();

  static const appName = 'EduBridge';
  static const figmaDesignSize = Size(375, 813);
  static const fontFamily = 'SF Pro';
  static const enableLogging = kDebugMode;
  static const proxyEnabled = false;
  static const sentryDsnUrl = '';
  static const playStoreUrl = '';
}
